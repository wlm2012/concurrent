spring:
  threads:
    virtual:
      enabled: true
  profiles:
    active: postgresql
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

server:
  port: 9904
